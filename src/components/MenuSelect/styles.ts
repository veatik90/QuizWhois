import { Box } from '@mui/material';
import styled from 'styled-components';

export const BoxForSelectStyled = styled(Box)`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 1;
`;
